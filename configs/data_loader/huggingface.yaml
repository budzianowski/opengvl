# Hugging Face (LeRobot) data loader
_target_: opengvl.data_loaders.huggingface.HuggingFaceDataLoader

dataset_name: ${dataset.dataset_name}
camera_index: ${dataset.camera_index}
num_frames: ${dataset.num_frames}
num_context_episodes: ${dataset.num_context_episodes}
shuffle: ${shuffle}
seed: ${seed}
# Limit total episodes to iterate (explicit in dataset config)
max_episodes: ${dataset.max_episodes}
